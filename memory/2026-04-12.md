# 2026-04-12

## Snapshot Backup — 03:01 UTC

**Status:** FAILED

**Error:**
```
ENOENT: no such file or directory, open '/home/john/.openclaw/workspace/config/google-oauth-token.json'
```

**Root cause:** Missing `google-oauth-token.json` — the OAuth token file hasn't been generated yet. The `google-oauth.json` (credentials) exists, but the token file from completed OAuth flow is missing.

**Fix needed:** Complete Google OAuth flow to generate `google-oauth-token.json`, or regenerate it.

**Script:** `/home/john/.openclaw/workspace/scripts/drive-openclaw-backup.js`