# March 4, 2026

## BC Container on GSPRO

- SSH access to GSPRO working (bot user)
- Docker works with Linux containers (Sonarr, Radarr, etc)
- Windows containers (BC) had HNS networking issues
- Fixed port conflict by using ports 18080, 17046-17049 instead of 8080
- Docker reset to factory defaults fixed the HNS issues
- BCContainerHelper creates containers but they keep exiting quickly
- Raw BC image needs special initialization - exits with "ServiceSettings.ps1" error
- BC container was created and running briefly but exits after ~4 minutes
- Need BCContainerHelper to properly initialize the container

## GSPRO Access
- SSH: bot@192.168.1.228
- Credentials: bot / BotPass123!
- Docker Hub token configured

## System Issues
- OpenClaw system very slow (exec tool timing out)
- Rebooted Beelink but still slow

## To Do
- Get BC container properly running with port mapping
- Test accessing from laptop: http://192.168.1.228:18080/BC

## Old Cron Results (from March 3)
- Daily Review: Calendar-to-Todoist sync failed due to missing calendar.readonly OAuth scope
- Both Calendar and Email-to-Todoist need re-authentication with proper scopes
