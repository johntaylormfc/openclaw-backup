# 2026-03-30

## 04:11 — Morning Heartbeat
- Dashboard, gateway, Docker all healthy
- Transmission starting (normal after restart)
- No kanban items needed attention

## 08:04 — Kanban Triage (3 new ideas)
- **IDEA-0159** Docker Image Update Watcher → Accepted → **OC-0045** (healthcheck covers security versions, not ARR image digests)
- **IDEA-0160** ARR Config Rolling Backup → Accepted → **OC-0046** (high priority, no existing backup solution)
- **IDEA-0161** Ollama AI Media Auto-Tagger → Rejected (Ollama stalled, dependency chain too long)

## Notes
- WhatsApp gateway connected as +447967688452

## NAS Hardlink Fix (15:27-16:20 UTC)

**Problem:** NAS exports each subdir as separate NFS share → hardlinks can't cross mount boundaries → ARR apps copy files instead of linking → slow imports

**Solution:** 
- NAS: Added `/volume1` single export alongside existing exports
- Beelink: Remounted as `/mnt/nas` pointing to `/volume1` (single NFS share)
- docker-compose: Updated all containers to use `/mnt/nas:/data` + symlinks via `/home/john/.openclaw/workspace/scripts/nas-entrypoint.sh`
- Hardlinks now work inside ALL containers (Sonarr, Radarr, Plex, Readarr, etc.)

**Files changed:**
- `/home/john/ARR/docker-compose.yml` - updated all NFS volume mounts
- `/home/john/.openclaw/workspace/scripts/nas-entrypoint.sh` - new entrypoint for symlink creation

**Testing:** ✅ Hardlinks verified in sonarr, radarr, plex, readarr

**Outstanding:** Old individual NFS exports still on NAS (can be cleaned up later)

## Evening (19:XX UTC)
- bxtest BC container was Dead (Windowsfilter VHD lock after Docker crash)
- Removed bxtest via `docker rm -f bxtest` (SSH as bot)
- Recreated bxtest: ports 18208/19210/19211 | user john / John1234
- Login failed initially — recreated container fresh and it worked
- glapproval and glapprovalwf both healthy and unaffected throughout
